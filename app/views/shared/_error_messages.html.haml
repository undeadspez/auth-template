- if @user.errors.any?
    #error_explanation
        .alert.alert-danger
            = "The form contains %s" % pluralize(@user.errors.count, "error")
        %ul
            - @user.errors.full_messages.each do |msg, i|
                %li= msg